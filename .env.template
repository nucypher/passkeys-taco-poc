# WebAuthn Configuration (for passkey registration)
# The Relying Party ID - typically your domain (e.g., "example.com" or "localhost" for development)
WEBAUTHN_RP_ID=localhost

# The expected origin for WebAuthn operations (e.g., "https://example.com" or "http://localhost:3000" for development)
WEBAUTHN_ORIGIN=http://localhost:3000